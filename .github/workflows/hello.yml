name: Responder a Issues
on:
  issues:
    types: [opened]
  workflow_dispatch:
permissions:
  issues: write
  contents: read
jobs:
  respond:
    runs-on: ubuntu-latest
    steps:
      - name: Comentar en el issue
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.issue.number }}
          body: "👋 ¡Gracias por tu aporte! El equipo revisará tu issu pronto."



